# Limit EAS upload to the monorepo (avoids home-directory git root on Windows)
node_modules
.git
.expo
dist
web-build
apps/admin
# Root Vite web app only — do not use bare "src" (would exclude apps/mobile/src)
/src
*.md
.env
.env.*
